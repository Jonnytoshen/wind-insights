# Mapbox（前端构建时注入）
VITE_MAPBOX_TOKEN=pk.your_mapbox_public_token_here

# 后端 CORS 白名单（生产环境务必改为真实域名）
CORS_ORIGINS=http://localhost,http://localhost:5173

# 任务超时时间（秒）
TASK_TIMEOUT_SECONDS=300

# 分析结果缓存 TTL（秒）
CACHE_TTL_SECONDS=1800
